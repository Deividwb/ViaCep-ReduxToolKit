import './styles.css';

const NavBar = () => {
  return (
    <header class="nav-container">
      <nav class="nav-main">
        <h1>Via CEP</h1>
        <div class="contact-link">Buscar Cep</div>        
      </nav>
    
    </header>    
    
  );
};

export default NavBar;
